# 🔥 Firebase Studio Prompt — Option A (Full Integration)

> Copy the entire content inside the code block below and paste it into Firebase Studio.

```text
# Project: Proto — Ambient Home Safety
# Non-contact AI ambient care solution web frontend for single elderly households
# This prompt sequentially implements 4 UI foundation tasks (UI-001 to UI-004).

────────────────────────────────────────────────
🛠️ TECH STACK & CONSTRAINTS (Strictly Enforced)
────────────────────────────────────────────────

- Framework: Next.js 14+ App Router only (Do not use Pages Router)
- Language: TypeScript strict mode
- Styling: Tailwind CSS + shadcn/ui component library
- Authentication: NextAuth.js (JWT strategy) — getServerSession(authOptions)
- Path Alias: @/ → project root (tsconfig.json paths)
- HTML: <html lang="ko"> required
- SEO: Use Next.js Metadata API — Do not modify <head> directly
- Server/Client Separation Principle:
  → Layout files must be Server Components (do not use "use client")
  → Hooks/events usage like usePathname(), useState(), onClick must be extracted into separate Client Components

Prerequisite: INFRA-001 (Next.js + Tailwind CSS + shadcn/ui initialization) is already completed.
npx create-next-app@latest (App Router, TypeScript, Tailwind CSS, ESLint options) and
npx shadcn-ui@latest init have been executed, so components.json and globals.css CSS variables exist.

────────────────────────────────────────────────
📁 Directory/File Structure to Create
────────────────────────────────────────────────

app/
├── layout.tsx                         ← [TASK 1] Root Layout (Server Component)
├── page.tsx                           ← [TASK 1] Landing Page
├── globals.css                        ← [TASK 1] Tailwind directives + shadcn/ui CSS variables
├── loading.tsx                        ← [TASK 1] Global Loading UI
├── not-found.tsx                      ← [TASK 1] Custom 404
├── error.tsx                          ← [TASK 1] Global Error Boundary
│
├── (guardian)/
│   ├── layout.tsx                     ← [TASK 2] Guardian Layout (Auth + Role protection)
│   ├── _components/
│   │   └── guardian-nav.tsx           ← [TASK 2] Guardian Navigation (Client Component)
│   ├── dashboard/
│   │   └── page.tsx                   ← [TASK 2] placeholder dashboard
│   └── reports/
│       └── page.tsx                   ← [TASK 2] placeholder reports
│
├── (admin)/
│   ├── layout.tsx                     ← [TASK 3] Admin Layout (RBAC protection)
│   ├── _components/
│   │   └── admin-nav.tsx             ← [TASK 3] Admin Navigation (Client Component)
│   └── dashboard/
│       ├── page.tsx                   ← [TASK 3] placeholder dashboard
│       └── events/
│           └── page.tsx               ← [TASK 3] placeholder event logs
│
components/
├── shared/
│   └── device-status-indicator.tsx    ← [TASK 4] Device Status Indicator
├── ui/                                ← shadcn/ui auto-generated (already exists)
│
types/
└── device.ts                          ← [TASK 4] DeviceStatus shared type

────────────────────────────────────────────────
📋 TASK 1: UI-001 — Root Layout + Landing Page
────────────────────────────────────────────────

[1-1] app/layout.tsx — Root Layout
──────────────────────────────────
- Implement as a Server Component (Do not use "use client")
- <html lang="ko"> setting required (for accessibility and internationalization)
- Ensure globals.css is imported inside <body>
- Export metadata using Next.js Metadata type:
  title: "Rooted — Ambient Home Safety"
  description: "AI-based non-contact ambient care solution. Guarding the safety of single elderly households 24/7."
- Apply Inter or Geist Sans font (next/font/google)
- Keep minimal structure wrapping {children}
- Add ThemeProvider only if supporting dark mode (optional)

[1-2] app/globals.css — Tailwind CSS + shadcn/ui variables
──────────────────────────────────────────────────────
- Ensure @tailwind base; @tailwind components; @tailwind utilities; directives are included
- shadcn/ui CSS variables must be defined in :root and .dark selectors:
  --background, --foreground, --card, --card-foreground,
  --primary, --primary-foreground, --secondary, --secondary-foreground,
  --muted, --muted-foreground, --accent, --accent-foreground,
  --destructive, --destructive-foreground, --border, --input, --ring, --radius
- ⚠️ These variables are auto-generated by npx shadcn-ui@latest init, do not modify them arbitrarily.

[1-3] app/page.tsx — Landing Page
────────────────────────────────────
Implementation Requirements:
- Center aligned, minimum height min-h-screen
- Service Name "Rooted" — Large typography (text-4xl md:text-6xl font-bold)
- Subtitle/Slogan: "AI-based Ambient Care Solution" (text-lg md:text-xl text-muted-foreground)
- Brief Service Introduction: "Non-contact monitoring of the daily lives of single elderly households, detecting anomalies and notifying guardians."
- 2 Login Buttons (Vertical or horizontal layout):
  ① Guardian Login — shadcn/ui <Button> (default variant) → <Link href="/login">
  ② Facility Admin Login — shadcn/ui <Button variant="outline"> → <Link href="/login">
- Responsive: Vertical stack on mobile, appropriate margins on tablet/desktop
- Design Reference: Minimal and clean SaaS landing page style

[1-4] app/not-found.tsx — Custom 404 Page
────────────────────────────────────────────
- Display large number "404"
- Message "Page Not Found" (페이지를 찾을 수 없습니다)
- Go back to Home button → <Link href="/"> (shadcn/ui Button)
- Center aligned, min-h-screen

[1-5] app/error.tsx — Global Error Boundary
─────────────────────────────────────────
- ⚠️ "use client" declaration required (Next.js Error Boundaries must be Client Components)
- Props: { error: Error & { digest?: string }; reset: () => void }
- Display message "Something went wrong" (문제가 발생했습니다)
- "Try again" button (다시 시도) → calls reset() function
- Center aligned layout

[1-6] app/loading.tsx — Global Loading UI
─────────────────────────────────────
- Use shadcn/ui Skeleton or CSS-based spinner
- Center on screen (flex items-center justify-center min-h-screen)
- Visually communicate that it is loading clearly

────────────────────────────────────────────────
📋 TASK 2: UI-002 — Guardian Layout (Guardian Portal)
────────────────────────────────────────────────

[2-1] app/(guardian)/layout.tsx — Server Component
───────────────────────────────────────────────────
Route Group Description:
- The (guardian) folder name is a Next.js Route Group and is not included in the URL
- Example: app/(guardian)/dashboard/page.tsx → URL is /dashboard

Auth Protection Logic (Server-Side):
- Check session using getServerSession(authOptions)
- If no session → redirect("/login")
- If session.user.role is not "GUARDIAN" → redirect("/") or show 403 page
- ⚠️ Client-side redirects alone are a security vulnerability — Server-side validation is mandatory

Layout Structure:
- flex-based layout: Sidebar (desktop) + Main content area
- On mobile, hide sidebar → replace with hamburger menu or bottom tab bar
- Render {children} in the main content area

Note: If NextAuth.js (AUTH-001) is not implemented yet, comment out auth-related code
or replace it with a mock session, but keep the code structure exactly the same.

[2-2] app/(guardian)/_components/guardian-nav.tsx — Client Component
─────────────────────────────────────────────────────────────────────
⚠️ "use client" declaration required

Functional Requirements:
- Use usePathname() hook to get current path → Highlight active menu
  (Active menu: visual distinction with bg-accent or font-semibold, etc.)
- Display user name/email retrieved from session (passed as props)

Menu Items:
┌──────────────────┬───────────────────────┬───────┐
│ Icon             │ Menu Name             │ Path   │
├──────────────────┼───────────────────────┼───────┤
│ 🏠 Home Icon     │ Home Dashboard (홈 대시보드) │ /dashboard │
│ 📊 FileText Icon │ Daily Report (일간 보고서)   │ /reports   │
│ ⚙️ Settings Icon │ Device Settings (디바이스 설정)│ (Disabled — to be added later) │
│ 🚪 LogOut Icon   │ Logout (로그아웃)           │ call signOut() │
└──────────────────┴───────────────────────┴───────┘

Icons: Use lucide-react icons (installed with shadcn/ui)
Logout: Call signOut() from next-auth/react → redirects to /login

Responsive Behavior:
- Desktop (≥ 768px): Left fixed sidebar (w-64, border-r, h-screen, fixed or sticky)
  → Vertical list of menu items, logout button at the bottom
- Mobile (< 768px): Slide menu based on shadcn/ui Sheet component
  → Hamburger icon button at the top (Menu icon)
  → Clicking opens Sheet slide from the left
  → Alternatively: Fixed bottom tab bar (fixed bottom-0) approach is also allowed
- Must work properly across the full range from 375px to 1280px

[2-3] Placeholder Pages
──────────────────────────
app/(guardian)/dashboard/page.tsx:
- <h1>Guardian Dashboard (보호자 대시보드)</h1>
- <p>Dashboard content will be displayed here.</p>
- (To be implemented later in TASK_FA-003)

app/(guardian)/reports/page.tsx:
- <h1>Daily Report (일간 보고서)</h1>
- <p>Report content will be displayed here.</p>
- (To be implemented later in TASK_RPT-002)

────────────────────────────────────────────────
📋 TASK 3: UI-003 — Admin Layout (Facility Admin Portal)
────────────────────────────────────────────────

[3-1] app/(admin)/layout.tsx — Server Component
─────────────────────────────────────────────────
Route Group:
- (admin) folder name is not included in the URL
- Example: app/(admin)/dashboard/page.tsx → URL is /dashboard
- ⚠️ Beware of URL collision with (guardian)/dashboard! Re-evaluate path design during actual implementation.
  → Recommendation: Change admin route to /admin/dashboard, or branch based on role within the layout

Role-Based Access Control (RBAC):
- Check session using getServerSession(authOptions)
- No session → redirect("/login")
- session.user.role !== "FACILITY_ADMIN" → redirect("/dashboard") or show 403 page
  (If the role is GUARDIAN, send them to the Guardian dashboard)
- ⚠️ Currently using simple string comparison for role; will be upgraded to middleware-based RBAC in Phase 2 SEC-004

Layout Structure:
- Same flex-based sidebar + main content pattern as Guardian Layout
- Recommended to extract common layout shell logic into components/shared/navigation.tsx for reuse

[3-2] app/(admin)/_components/admin-nav.tsx — Client Component
───────────────────────────────────────────────────────────────
⚠️ "use client" declaration required

Functional Requirements:
- Highlight active menu based on usePathname()
- Display "Facility Admin" (시설 관리자) role badge:
  → shadcn/ui Badge variant="secondary" or custom badge
  → Placed at the top of the sidebar, next to the user name
- Show admin-only menu items (different from Guardian menu)

Menu Items:
┌─────────────────────┬───────────────────────────┬──────────────────────────┐
│ Icon                │ Menu Name                 │ Path                     │
├─────────────────────┼───────────────────────────┼──────────────────────────┤
│ 📊 LayoutDashboard  │ Real-time Dashboard (실시간 대시보드)│ /(admin)/dashboard       │
│ 📋 List Icon        │ Event Logs (이벤트 로그)      │ /(admin)/dashboard/events│
│ 🔧 Wrench Icon      │ Device Management (디바이스 관리)│ (Disabled — Phase 3+)    │
│ 🚪 LogOut Icon      │ Logout (로그아웃)            │ call signOut()           │
└─────────────────────┴───────────────────────────┴──────────────────────────┘

Responsive Behavior:
- Same pattern as Guardian Layout (Desktop sidebar / Mobile Sheet menu)
- Must work properly across the full range from 375px to 1280px

[3-3] Placeholder Pages
──────────────────────────
app/(admin)/dashboard/page.tsx:
- <h1>Facility Admin Dashboard (시설 관리자 대시보드)</h1>
- <p>Real-time monitoring dashboard will be displayed here.</p>
- (To be implemented later in TASK_DASH-001)

app/(admin)/dashboard/events/page.tsx:
- <h1>Event Logs (이벤트 로그)</h1>
- <p>Event log search and list will be displayed here.</p>
- (To be implemented later in TASK_DASH-005)

────────────────────────────────────────────────
📋 TASK 4: UI-004 — DeviceStatusIndicator Shared Component
────────────────────────────────────────────────

[4-1] types/device.ts — Shared Type Definition
───────────────────────────────────────
export type DeviceStatus = 'ACTIVE' | 'INACTIVE' | 'MAINTENANCE';

This type will be reused throughout the project.
Do not redefine DeviceStatus in other files, import it from this file.

[4-2] components/shared/device-status-indicator.tsx
────────────────────────────────────────────────────
⚠️ "use client" declaration

Props Interface:
interface DeviceStatusIndicatorProps {
  status: DeviceStatus;      // Required — device status
  size?: 'sm' | 'md' | 'lg'; // Optional — indicator size (default: 'md')
  showLabel?: boolean;       // Optional — whether to show text label (default: false)
  pulse?: boolean;           // Optional — animate-pulse effect if ACTIVE (default: false)
}

Status Color + Label Mapping:
┌──────────────┬──────────────────┬───────────────┐
│ status       │ Tailwind Class   │ Korean Label  │
├──────────────┼──────────────────┼───────────────┤
│ ACTIVE       │ bg-green-500     │ "활성"         │
│ INACTIVE     │ bg-red-500       │ "오프라인"      │
│ MAINTENANCE  │ bg-yellow-500    │ "점검 중"       │
└──────────────┴──────────────────┴───────────────┘

Size Mapping:
┌──────────────┬──────────────────┐
│ size         │ Tailwind Class   │
├──────────────┼──────────────────┤
│ sm           │ w-2 h-2          │
│ md (default) │ w-3 h-3          │
│ lg           │ w-4 h-4          │
└──────────────┴──────────────────┘

Implementation Details:
- Circular indicator: rounded-full + bg color by status
- showLabel={true} → show Korean text label to the right of the color dot (inline-flex, gap-2, items-center)
- showLabel={false} (default) → show only the dot
- pulse={true} AND status="ACTIVE" → add animate-pulse Tailwind class to the dot
- pulse={true} but status is not ACTIVE → ignore pulse

Accessibility (Required):
- role="status" attribute on the top-level element of the component
- aria-label="Device Status: {Korean Label}" (e.g., aria-label="디바이스 상태: 활성")
- ⚠️ Do not convey state by color alone — aria-label is mandatory

Component Characteristics:
- Pure Presentational Component: no internal state, no API calls
- Rendering determined solely by Props
- Reused by importing into both Guardian and Admin portals

Usage Examples:
<DeviceStatusIndicator status="ACTIVE" showLabel={true} size="md" pulse={true} />
<DeviceStatusIndicator status="INACTIVE" size="sm" />
<DeviceStatusIndicator status="MAINTENANCE" showLabel={true} size="lg" />

────────────────────────────────────────────────
✅ Post-Implementation Verification Checklist
────────────────────────────────────────────────

Check all 10 items below:

1. [Build] npm run build completes with 0 errors
2. [Types] 0 TypeScript type errors
3. [Lint] 0 ESLint warnings/errors
4. [Landing] Accessing http://localhost:3000 → Renders "Rooted" Landing Page
5. [Login Navigation] Clicking login buttons on Landing page → Routes to /login
6. [404] Accessing /nonexistent → Renders custom 404 page
7. [Metadata] Verify browser tab title is "Rooted — Ambient Home Safety"
8. [Guardian Auth] Accessing /dashboard unauthenticated → Redirects to /login
9. [Admin RBAC] Accessing Admin route with GUARDIAN role → Blocked and appropriately redirected
10. [DeviceStatusIndicator] All combinations of 3 statuses × 3 sizes × label ON/OFF render correctly

────────────────────────────────────────────────
⚠️ Cautions and Constraints
────────────────────────────────────────────────

1. Route Groups: (guardian) and (admin) are Next.js Route Groups and are not included in the URL.
   Actual URLs accessed will be /dashboard, /reports, etc.

2. Server/Client Separation:
   - layout.tsx files → Server Components (do not use "use client")
   - Usage of usePathname(), useState(), signOut() → Must be extracted to separate Client Component files

3. globals.css:
   Keep the shadcn/ui CSS variables auto-generated by npx shadcn-ui@latest init.
   Arbitrary modifications may break shadcn/ui component styling.

4. If Authentication is Not Implemented Yet:
   If NextAuth.js (AUTH-001) is not yet set up,
   comment out getServerSession() related code or replace it with a mock like below:
   const session = { user: { name: "Test User", email: "test@example.com", role: "GUARDIAN" } };
   Keep the code structure so that you only need to uncomment it when actual auth is applied.

5. Extracting Common Logic:
   Since the sidebar shell (layout frame) logic is similar for Guardian Nav and Admin Nav,
   it is recommended to follow the DRY principle by extracting common components to components/shared/sidebar-shell.tsx.

6. Placeholder Pages:
   dashboard/page.tsx, reports/page.tsx, etc., should contain minimal <h1> + <p> tags.
   Actual content will be implemented in subsequent tasks (DASH-001, RPT-002, etc.).

Implement the above specifications in order (TASK 1 → 2 → 3 → 4).
After completing each TASK, verify that the corresponding files work correctly before proceeding to the next.
```
